---
# Declarative DNS rewrites for AdGuard Home.
# Add entries to adguard_dns_rewrites in group_vars/townhaus_caddy/ to manage them here.
# All entries point to beelink so Caddy handles the reverse proxy.

- name: Resolve AdGuard credentials from 1Password
  ansible.builtin.command:
    argv: [op, read, "{{ item.ref }}"]
  delegate_to: localhost
  loop:
    - { ref: "{{ adguard_user_ref }}", var: user }
    - { ref: "{{ adguard_pass_ref }}", var: pass }
  register: adguard_dns_creds_result
  changed_when: false
  no_log: true

- name: Store AdGuard credentials
  ansible.builtin.set_fact:
    adguard_dns_user: "{{ adguard_dns_creds_result.results[0].stdout }}"
    adguard_dns_pass: "{{ adguard_dns_creds_result.results[1].stdout }}"
  no_log: true

- name: Fetch current DNS rewrites
  ansible.builtin.uri:
    url: "http://localhost:{{ adguard_admin_port }}/control/rewrite/list"
    method: GET
    url_username: "{{ adguard_dns_user }}"
    url_password: "{{ adguard_dns_pass }}"
    force_basic_auth: true
    return_content: true
  register: adguard_dns_rewrites_result

- name: Delete rewrites with wrong answer
  ansible.builtin.uri:
    url: "http://localhost:{{ adguard_admin_port }}/control/rewrite/delete"
    method: POST
    url_username: "{{ adguard_dns_user }}"
    url_password: "{{ adguard_dns_pass }}"
    force_basic_auth: true
    body_format: json
    body:
      domain: "{{ item.domain }}"
      answer: "{{ (adguard_dns_rewrites_result.json | selectattr('domain', 'equalto', item.domain) | list | first).answer }}"
    status_code: 200
  loop: "{{ adguard_dns_rewrites }}"
  when: >
    (adguard_dns_rewrites_result.json | selectattr('domain', 'equalto', item.domain) | list | length > 0) and
    (adguard_dns_rewrites_result.json | selectattr('domain', 'equalto', item.domain) | list | first).answer != (item.answer | default(adguard_dns_answer))
  loop_control:
    label: "{{ item.domain }}"

- name: Add missing DNS rewrites
  ansible.builtin.uri:
    url: "http://localhost:{{ adguard_admin_port }}/control/rewrite/add"
    method: POST
    url_username: "{{ adguard_dns_user }}"
    url_password: "{{ adguard_dns_pass }}"
    force_basic_auth: true
    body_format: json
    body:
      domain: "{{ item.domain }}"
      answer: "{{ item.answer | default(adguard_dns_answer) }}"
    status_code: 200
  loop: "{{ adguard_dns_rewrites }}"
  when: >
    (adguard_dns_rewrites_result.json | selectattr('domain', 'equalto', item.domain) | list | length == 0) or
    (adguard_dns_rewrites_result.json | selectattr('domain', 'equalto', item.domain) | list | first).answer != (item.answer | default(adguard_dns_answer))
  loop_control:
    label: "{{ item.domain }}"
