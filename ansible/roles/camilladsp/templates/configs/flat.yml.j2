description: null
title: Reference

devices:
  capture:
    channels: 2
    device: "{{ camilladsp_capture_device }}"
    format: "{{ camilladsp_format }}"
    type: Alsa
  chunksize: {{ camilladsp_chunksize }}
  multithreaded: true
  playback:
    channels: 2
    device: "{{ camilladsp_playback_device }}"
    format: "{{ camilladsp_format }}"
    type: Alsa
  samplerate: {{ camilladsp_samplerate }}

filters: {}

pipeline: []
