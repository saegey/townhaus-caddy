---
- name: Add beszel-hub service to docker-compose
  ansible.builtin.debug:
    msg: >-
      Beszel Hub is managed as part of the Docker Compose stack on beelink.
      Run the townhaus_caddy deploy playbook to apply docker-compose changes.
